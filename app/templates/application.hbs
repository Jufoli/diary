<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header col-sm-6 col-md-6">
      Browser-Tagebuch
    </div>
	{{#if isLoggedIn}}
	<div class="navbar-status col-sm-offset-2 col-md-offset-2 col-sm-4 col-md-4">
		Eingeloggt als: {{name}} (<a {{action 'logout'}}>Logout</a>)
	</div>
	{{/if}}
  </div>
</nav>

{{outlet}}
